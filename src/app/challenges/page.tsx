"use client"; import { useEffect, useMemo, useState } from "react"; import { AppHeader, AppShell } from "@/components/app-shell"; import { Button, ChallengeCard } from "@/components/ui"; import { challengeApi, Challenge } from "@/lib/api"; import { Search } from "lucide-react";
export default function Challenges(){const [items,setItems]=useState<Challenge[]>([]),[q,setQ]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{challengeApi.list().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const result=useMemo(()=>items.filter(x=>`${x.title} ${x.category}`.toLowerCase().includes(q.toLowerCase())),[items,q]);return <AppShell><AppHeader eyebrow="Trouvez votre prochain duel" title="LES DÉFIS."><Button href="/challenges/create">Créer un défi</Button></AppHeader><div className="p-5 md:p-10"><div className="mb-8 flex flex-wrap gap-3"><label className="flex min-w-64 flex-1 items-center gap-2 border border-[#D7DFD0] bg-white px-3"><Search size={16}/><input aria-label="Rechercher un défi" value={q} onChange={e=>setQ(e.target.value)} placeholder="Rechercher par thème..." className="w-full py-3 text-sm outline-none"/></label></div>{loading&&<p className="text-sm text-slate-500">Chargement des défis…</p>}{error&&<p role="alert" className="border-l-2 border-red-600 bg-red-50 p-3 text-sm text-red-700">{error}</p>}{!loading&&!error&&!result.length&&<div className="border border-dashed border-[#D7DFD0] bg-white p-12 text-center"><b className="display text-3xl">L’ARÈNE ATTEND SON PREMIER DÉFI.</b><p className="mt-3 text-sm text-slate-500">Créez une compétition pour commencer.</p></div>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{result.map(x=><ChallengeCard challenge={x} key={x.id}/>)}</div></div></AppShell>}

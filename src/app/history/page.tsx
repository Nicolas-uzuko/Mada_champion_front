"use client";
import { useEffect,useState } from "react"; import { AppHeader,AppShell } from "@/components/app-shell"; import { HistoryItem,userApi } from "@/lib/api";
export default function History(){const [rows,setRows]=useState<HistoryItem[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{userApi.history().then(setRows).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <AppShell><AppHeader eyebrow="Votre parcours" title="MA CARRIÈRE."/><div className="p-5 md:p-10">{loading&&<p className="text-sm text-slate-500">Chargement de votre historique…</p>}{error&&<p role="alert" className="border-l-2 border-red-600 bg-red-50 p-3 text-sm text-red-700">{error}</p>}{!loading&&!error&&!rows.length&&<p className="border border-dashed border-[#D7DFD0] bg-white p-10 text-center text-sm text-slate-500">Votre carrière commencera après votre premier défi terminé.</p>}{rows.map((r,i)=><div className="grid grid-cols-[22px_1fr_auto] gap-4 border-b border-[#D7DFD0] py-6" key={r.id}><span className={`mt-1 h-3 w-3 rounded-full ${i===0?"bg-[#679436]":"bg-[#D7DFD0]"}`}/><div><b>Défi terminé</b><p className="mt-1 text-xs text-slate-500">{new Intl.DateTimeFormat("fr-MG",{dateStyle:"medium"}).format(new Date(r.played_at))} · {r.correct_answers} bonnes réponses</p></div><div className="text-right"><b>{r.final_rank}e</b><p className="text-xs text-slate-500">{r.final_score} pts</p></div></div>)}</div></AppShell>}
